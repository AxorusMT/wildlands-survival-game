{
  "name": "wildlands-survival-game",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.ts --bundle --format=iife --target=es2022 --outfile=dist/wildlands.js",
    "watch": "npm run build -- --watch",
    "typecheck": "tsc",
    "format": "prettier --write src tests index.html style.css book.css README.md package.json tsconfig.json",
    "format:check": "prettier --check src tests index.html style.css book.css README.md package.json tsconfig.json",
    "test": "node --test \"tests/**/*.test.js\""
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "esbuild": "^0.28.2",
    "prettier": "^3.9.9",
    "typescript": "^7.0.2"
  },
  "type": "module"
}
